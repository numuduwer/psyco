<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="MemberShop">
  	<insert id="isertMemberShop" parameterType="MemberShopDTO">
  		insert into shop values(#{shop_num},#{shop_name},#{shop_phone},#{operating_time},#{address},#{origin},#{takeout},#{shop_img},#{license_number},sysdate,sysdate,#{status},#{approve_status},#{member_num})
  	</insert>
  
  </mapper>