<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="member">
  	<select id="existUserCheck" parameterType="String" resultType="_int">
  		select count(*) from member where email=#{value}
  	</select>
  	
  	<select id="getMemberProfile" parameterType="String" resultType="memberDTO">
  		select * from member where email=#{value}
  	</select>
  	
  	<insert id="insertMember" parameterType="memberDTO">
  		<if test="pw == null">
  			insert into member(member_num, member_Id, name, birth, phonenum, gender, nickname, email, purchase_count, business_license) values(member_seq.nextval, #{member_Id}, #{name}, #{birth}, #{phoneNum}, #{gender}, #{nickname}, #{email}, #{purchase_count}, #{business_license})
  		</if>
  		<if test="pw != null">
  			insert into member(member_num, member_Id, pw, name, birth, phonenum, gender, nickname, email, purchase_count, business_license) values(member_seq.nextval, #{member_Id}, #{pw}, #{name}, #{birth}, #{phoneNum}, #{gender}, #{nickname}, #{email}, #{purchase_count}, #{business_license})
  		</if>
  	</insert>
  	
  	<select id="loginCheck" parameterType="hashmap" resultType="_int">
  		select count(*) from member where member_Id=#{member_Id} and pw=#{pw}
  	</select>
  	
  	<!--멤버 정보 가져오기 -동윤-  -->
  	<select id="memberInfo" parameterType="String" resultType="MemberDTO">
  		select * from member where member_id=#{value}
  	</select>
  	<!-- 멤버 닉네임 가져오기 동윤 -->
  	<select id="getNickName" parameterType="String" resultType="String">
  		select nickname from member where member_in=#{value}
  	</select>
  </mapper>